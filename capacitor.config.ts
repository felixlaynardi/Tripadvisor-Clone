import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'TripAdvisor-Clone',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
